export enum NAVIGATION_URLS {
  HOME = '/',
  METRIC = '/metric',
}

export enum COLORS {
  BLACK = '#000000',
  BLUE = '#2563EB',
}

export enum METRICS_NAMES {
  EXPENSES = 'expenses',
  REVENUE = 'revenue',
  EBITDAR = 'ebitdar',
}
